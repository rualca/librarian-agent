# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# LLM Provider: "groq" (default, free & fast) or "openai"
LLM_PROVIDER=groq

# --- Groq (default) ---
GROQ_API_KEY=your-groq-api-key
GROQ_MODEL=meta-llama/llama-4-scout-17b-16e-instruct
GROQ_TRANSCRIPTION_MODEL=whisper-large-v3-turbo

# --- OpenAI (alternative) ---
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4o
OPENAI_TRANSCRIPTION_MODEL=whisper-1

# Vault path (path inside the Docker container)
VAULT_PATH=/vault

# Authorized Telegram user IDs (comma-separated, for security)
AUTHORIZED_USERS=

# Language for bot responses (es/en)
BOT_LANGUAGE=es

# ============================================
# OpenCode Integration (for /oc command)
# ============================================
# Set these in your environment or docker-compose.yml
# OpenCode uses these API keys for its AI coding agent
#
# ZHIPU_API_KEY=your-zhipu-api-key
# OPENAI_API_KEY=your-openai-api-key  (already set above if using OpenAI)
# ANTHROPIC_API_KEY=your-anthropic-api-key
#
# See https://opencode.ai/docs/providers/ for all supported providers
